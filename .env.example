# Copy this to .env and fill in real values.
# NEVER commit .env to git.

DB_USER=flask_user
DB_PASSWORD=change_me_strong_password
DB_HOST=your-rds-endpoint.rds.amazonaws.com   # use "db" for local docker-compose
DB_PORT=5432
DB_NAME=notesdb
